{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "business_development",
        "clinical",
        "finance",
        "read_only"
      ],
      "default": "read_only"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}